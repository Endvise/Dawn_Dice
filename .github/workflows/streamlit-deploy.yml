# Streamlit Cloud는 GitHub와 자동 연동됩니다.
# 이 워크플로우 파일은 사용하지 않습니다. (streamlit-app-action@v1 지원 중단)
#
# 배포 방법: GitHub에 푸시하면 Streamlit Cloud가 자동으로Redeploy됩니다.
#
# name: Streamlit Cloud Deploy
#
# on:
#   push:
#     branches: [master]
#   workflow_dispatch:
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to Streamlit Cloud
#         uses: streamlit/streamlit-app-action@v1
#         with:
#           repo-name: ${{ github.repository }}
#           repo-owner: ${{ github.repository_owner }}
#           app-name: dawndice
#           branch: master
#           main-file-path: dice_app/app.py
